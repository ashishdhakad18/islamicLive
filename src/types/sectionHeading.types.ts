export interface SectionHeadingData {
  heading: string;
  subheading?: string;
  description?: string;
  align?: "left" | "center" | "right";
  theme?: "dark" | "light";
  buttonText?: string;
  buttonLink?: string;
}
